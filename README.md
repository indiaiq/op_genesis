# OP Genesis Landing Page

Static-export Next.js landing page for OP Genesis Healthcare Logistics.

## Build

```powershell
npm install
npm run build
```

## Preview Without A Local Server

```powershell
npm run preview:file
```

This opens `out/index.html` directly in your browser. You can also open that file from VS Code after running the build.
